<%= form_for @teacher do |f| %>
 
  <% if @teacher.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@teacher.errors.count, "error") %> prohibited
        this teacher from being saved:
      </h2>
      <ul>
        <% @teacher.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 <p>
    <%= f.label :school_id %><br>
    <%= select_tag 'teacher[school_id]', options_from_collection_for_select(School.all, :id, :name) %>
 </p>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
   <%= f.label :gender %><br>
    <select name="teacher[gender]" id="teacher_gender">
        <option value="Male" selected>Male</option>
        <option value="Female">Female</option>
    </select>
  </p>
  <p>
    <%= f.label :phone_no%><br>
    <%= f.text_field :phone_no%>
  </p>
 <p>
    <%= f.label :proficiency_subject%><br>
    <%= f.text_field :proficiency_subject%>
 </p>
  
  
  <p>
    <%= f.submit %>
  </p>
 
<% end %>